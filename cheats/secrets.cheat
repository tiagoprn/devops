% security, keys, secrets, gpg, env, OPENAI

# trigger nvim with OPENAI key setup
export OPENAI_API_KEY=$(pass api-keys/OPENAI) && nvim

# setup/export OPENAI key environment variable
export OPENAI_API_KEY=$(pass api-keys/OPENAI)
