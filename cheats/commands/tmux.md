**rename session**: `<prefix>?` (this also shows help for the default mappings of the other commands)
**create new session**: `C-ENTER` (it will ask for the session name)
**load tmuxp session**: `Shift-ENTER`
