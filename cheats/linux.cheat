% linux

# add user to a group:
sudo usermod -aG <groups> ${USER}

# list current user groups:
id -nG

# get user id
id -u

# get user group id
id -g

# figlet
echo "$(hostname)" | figlet -cptk

# do not show blank lines on output
grep '\S'

# tee: redirect command output (stdout and stderr) to file or another command.
<command> |& tee -a <file-or-command>
