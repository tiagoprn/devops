session_name: devops_ansible
windows:
  - window_name: main
    layout: even-vertical
    start_directory: /storage/src/devops 
    panes:
      - cd docker_composes/ansible && clear
      - cd docker_composes/ansible && clear
      - cd ansible_playbooks && clear
      - cd ansible_playbooks && clear
      - sudo watch -n 1 docker ps  
