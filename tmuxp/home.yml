session_name: HOME
windows:
  - window_name: main
    layout: even-vertical
    start_directory: /storage/src
    panes:
      - pyenv deactivate && cd dot_files && clear
      - pyenv deactivate && cd devops && clear
      - pyenv deactivate && cd ~ && clear

